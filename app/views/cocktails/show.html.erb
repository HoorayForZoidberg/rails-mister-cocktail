<h1><%= @cocktail.name %></h1>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li>
      <span><%= "#{dose.ingredient.name}:  #{dose.description}" %></span>
      <!-- add dose delete method here -->
      </li>
  <% end %>
</ul>
<br>
<%= link_to "Add an ingredient", new_cocktail_dose_path(@cocktail) %>
<br>
<%= link_to "Back to list", cocktails_path %>

